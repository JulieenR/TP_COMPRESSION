function [image_out] = dct(image_in) 
    D = dctmtx(8);
    
    dim = size(image_in);
    
    for i = 1:dim(3)
        xtmp = x(:,:,i);
        if i==1
            y = D*xtmp*D';
        else    
            y(:,:,i) = D*xtmp*D';
        end
    end
end